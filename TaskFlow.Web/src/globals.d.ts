// A fresh value baked into the served bundle every time the dev server (or a real build) starts.
// See vite.config.ts's `define` block for where this is set, and lib/devAuthReset.ts for why.
declare const __APP_BOOT_ID__: string
